# [job-queue] is not optional: without it PTB silently ignores the
# ConversationHandler's conversation_timeout, and a cashier who wanders off
# mid-sale stays stuck in the sell flow forever.
python-telegram-bot[job-queue]==21.9
httpx==0.27.*
python-dotenv==1.0.*
